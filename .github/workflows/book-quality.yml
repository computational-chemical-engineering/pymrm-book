name: Jupyter Book CI Checks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: book-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  book-quality:
    name: Validate book build and content quality
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ./pymrm
          pip install -r requirements.txt
          pip install codespell nbconvert ipykernel
          python -m ipykernel install --user --name python3

      - name: Install language checker
        run: |
          sudo apt-get update
          sudo apt-get install -y nodejs npm
          npm install --global alex

      - name: Execute lecture note notebooks
        timeout-minutes: 20
        run: python scripts/execute_book_notebooks.py

      - name: Regenerate API docs
        run: python3 scripts/generate_function_list.py

      - name: Build Jupyter Book (HTML)
        timeout-minutes: 10
        run: jupyter-book build --html --ci
        env:
          BASE_URL: /pymrm-book

      - name: Check links
        continue-on-error: true
        timeout-minutes: 10
        run: jupyter-book build --html --check-links --ci

      - name: Spelling check (codespell)
        run: |
          codespell README.md content _static myst.yml \
            --skip="*.png,*.svg,*.ipynb,*.tex,*.bib,*.css,*.js" \
            --ignore-words=.codespell-ignore-words.txt

      - name: Language check (alex)
        continue-on-error: true
        run: |
          mapfile -t markdown_files < <(find content -type f -name "*.md" | sort)
          alex README.md "${markdown_files[@]}"
