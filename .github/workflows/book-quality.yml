name: Jupyter Book Quality Checks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: book-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  book-quality:
    name: Build, lint, spell, language, and link checks
    runs-on: ubuntu-latest
    timeout-minutes: 60
    container:
      image: python:3.12

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends git curl nodejs npm
          rm -rf /var/lib/apt/lists/*

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ./pymrm
          pip install -r requirements.txt
          pip install "jupyter-book==0.15.1" "sphinx==5.0.2" "sphinx-autodoc-typehints<1.19" codespell

      - name: Install language checker
        run: npm install --global alex

      - name: Build Jupyter Book (HTML)
        run: |
          jupyter-book clean .
          jupyter-book build .

      - name: Check links
        run: jupyter-book build . --builder linkcheck

      - name: Spelling check (codespell)
        run: |
          codespell README.md content _config.yml _toc.yml \
            --skip="*.png,*.svg,*.ipynb,*.tex,*.bib,*.css,*.js"

      - name: Language check (alex)
        continue-on-error: true
        run: |
          mapfile -t markdown_files < <(find content -type f -name "*.md" | sort)
          alex README.md "${markdown_files[@]}"
